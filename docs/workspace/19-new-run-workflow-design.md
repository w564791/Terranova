# New Run工作流设计

> **文档版本**: v1.0  
> **创建日期**: 2025-10-11  
> **状态**: 设计方案  
> **优先级**: P0（核心功能）

## 📋 概述

本文档描述New Run的完整工作流设计，包括任务创建、资源添加、版本管理等核心流程。

## 🎯 设计目标

1. 提供灵活的任务创建方式
2. 支持资源级别的版本管理
3. 复用现有的Module表单系统
4. 符合用户操作习惯

## 🔄 完整工作流

### 流程图

```
┌─────────────┐
│  New run    │
│   对话框     │
└──────┬──────┘
       │
       ├─→ Plan ────────────────→ 执行Plan任务
       │
       ├─→ Plan + Apply ────────→ 执行Plan+Apply流程
       │
       └─→ 添加资源
           │
           ├─→ 选择Module
           │
           ├─→ 填写表单
           │
           ├─→ 预览配置
           │
           ├─→ 继续添加 ──┐
           │              │
           └─→ 完成 ──────┤
                          │
                          ├─→ 选择Plan ────→ 创建资源版本 → 执行Plan
                          │
                          └─→ 选择Plan+Apply → 创建资源版本 → 执行Plan+Apply
```

## 📝 详细设计

### 1. New Run对话框

#### UI设计

```
┌─────────────────────────────────────────┐
│  Start a new run                    ×   │
├─────────────────────────────────────────┤
│                                          │
│  Choose run type:                        │
│                                          │
│  ○ Plan                                  │
│    Execute plan to preview changes       │
│                                          │
│  ○ Plan and apply                        │
│    Execute plan and apply changes        │
│                                          │
│  ○ Add resources                         │
│    Add modules to workspace              │
│                                          │
├─────────────────────────────────────────┤
│              [Cancel]  [Start Run]       │
└─────────────────────────────────────────┘
```

#### 选项说明

**Plan**:
- 直接执行Plan任务
- 使用workspace现有的资源
- 不创建新的资源版本
- 适用场景：查看当前配置的变更

**Plan + Apply**:
- 执行完整的Plan+Apply流程
- Plan后是否自动Apply取决于workspace的Apply Method设置
- 使用workspace现有的资源
- 不创建新的资源版本
- 适用场景：部署现有配置

**添加资源**:
- 进入添加资源流程
- 从Module库选择Module
- 填写表单并提交
- 创建新的资源版本
- 然后执行Plan或Plan+Apply

### 2. 添加资源流程

#### 步骤1: 选择Module

```
┌─────────────────────────────────────────┐
│  Add Resources                      ×   │
├─────────────────────────────────────────┤
│  Select modules from library:            │
│                                          │
│  ┌────────────────────────────────────┐ │
│  │ 🔍 Search modules...               │ │
│  └────────────────────────────────────┘ │
│                                          │
│  ┌────────────────────────────────────┐ │
│  │ ☑ AWS S3 Bucket                    │ │
│  │   Create and manage S3 buckets     │ │
│  └────────────────────────────────────┘ │
│                                          │
│  ┌────────────────────────────────────┐ │
│  │ ☐ AWS VPC                          │ │
│  │   Create VPC and subnets           │ │
│  └────────────────────────────────────┘ │
│                                          │
├─────────────────────────────────────────┤
│              [Cancel]  [Next]            │
└─────────────────────────────────────────┘
```

#### 步骤2: 填写表单

```
┌─────────────────────────────────────────┐
│  Configure: AWS S3 Bucket           ×   │
├─────────────────────────────────────────┤
│  Resource Name: *                        │
│  ┌────────────────────────────────────┐ │
│  │ my_bucket                          │ │
│  └────────────────────────────────────┘ │
│                                          │
│  [动态表单字段...]                        │
│  （复用现有的DynamicForm组件）            │
│                                          │
├─────────────────────────────────────────┤
│  [Back]  [Add Another]  [Preview]       │
└─────────────────────────────────────────┘
```

#### 步骤3: 预览和提交

```
┌─────────────────────────────────────────┐
│  Preview Resources                  ×   │
├─────────────────────────────────────────┤
│  Resources to be added:                  │
│                                          │
│  1. aws_s3_bucket.my_bucket              │
│     - bucket: my-unique-bucket           │
│     - acl: private                       │
│                                          │
│  2. aws_vpc.main                         │
│     - cidr_block: 10.0.0.0/16           │
│                                          │
├─────────────────────────────────────────┤
│  Choose action:                          │
│  ○ Plan only                             │
│  ○ Plan and apply                        │
│                                          │
├─────────────────────────────────────────┤
│  [Back]  [Add More]  [Submit]           │
└─────────────────────────────────────────┘
```

### 3. 后端处理流程

#### 添加资源并执行任务

```go
// 1. 创建资源记录
for _, moduleData := range submittedModules {
    resource := CreateResource(workspaceID, moduleData)
    
    // 2. 创建资源版本
    version := CreateResourceVersion(resource.ID, moduleData.Config, "create")
    
    // 3. 更新资源的当前版本
    resource.CurrentVersionID = version.ID
}

// 4. 创建任务
if runType == "plan" {
    task := CreatePlanTask(workspaceID)
} else if runType == "plan_and_apply" {
    task := CreatePlanTask(workspaceID)
    // Apply Method决定是否自动创建Apply任务
}
```

#### 直接执行任务（不添加资源）

```go
// 直接使用现有资源创建任务
if runType == "plan" {
    task := CreatePlanTask(workspaceID)
} else if runType == "plan_and_apply" {
    task := CreatePlanTask(workspaceID)
    // Apply Method决定是否自动创建Apply任务
}
```

### 4. 版本管理

#### 资源版本创建时机

**创建新版本**：
- 用户添加新资源
- 用户修改现有资源
- 用户回滚资源

**不创建新版本**：
- 直接执行Plan任务
- 直接执行Plan+Apply任务

#### 版本获取逻辑

```go
// 执行任务时，从资源聚合生成配置
func GenerateMainTF(workspaceID uint) {
    // 获取所有激活资源的当前版本
    resources := GetActiveResources(workspaceID)
    
    for _, resource := range resources {
        // 使用resource.CurrentVersion.TFCode
        mergeTFCode(mainTF, resource.CurrentVersion.TFCode)
    }
}
```

### 5. Apply Method设置

#### 配置位置

在workspace设置中：
- **Auto apply**: 自动执行Apply
- **Manual apply**: 需要手动确认

#### 行为说明

**Plan + Apply流程**：
1. 创建Plan任务
2. Plan执行成功后：
   - 如果Auto apply = On → 自动创建Apply任务
   - 如果Auto apply = Off → 等待用户手动创建Apply任务

## 🎨 前端实现

### 1. New Run对话框组件

```typescript
interface NewRunDialogProps {
  workspaceId: string;
  onClose: () => void;
}

const NewRunDialog: React.FC<NewRunDialogProps> = ({ workspaceId, onClose }) => {
  const [runType, setRunType] = useState<'plan' | 'plan_and_apply' | 'add_resources'>('plan');
  
  const handleSubmit = () => {
    if (runType === 'add_resources') {
      // 跳转到添加资源页面
      navigate(`/workspaces/${workspaceId}/add-resources`);
    } else {
      // 直接创建任务
      createTask(workspaceId, runType);
    }
  };
  
  return (
    <Dialog>
      <RadioGroup value={runType} onChange={setRunType}>
        <Radio value="plan">Plan</Radio>
        <Radio value="plan_and_apply">Plan and apply</Radio>
        <Radio value="add_resources">Add resources</Radio>
      </RadioGroup>
      <Button onClick={handleSubmit}>Start Run</Button>
    </Dialog>
  );
};
```

### 2. 添加资源页面

```typescript
const AddResourcesPage: React.FC = () => {
  const [selectedModules, setSelectedModules] = useState<Module[]>([]);
  const [resourceConfigs, setResourceConfigs] = useState<ResourceConfig[]>([]);
  const [step, setStep] = useState<'select' | 'configure' | 'preview'>('select');
  
  // 步骤1: 选择Module
  // 步骤2: 填写表单（复用DynamicForm）
  // 步骤3: 预览并选择Plan或Plan+Apply
  
  const handleSubmit = async (runType: 'plan' | 'plan_and_apply') => {
    // 1. 创建资源和版本
    for (const config of resourceConfigs) {
      await api.post(`/workspaces/${workspaceId}/resources`, config);
    }
    
    // 2. 创建任务
    if (runType === 'plan') {
      await api.post(`/workspaces/${workspaceId}/tasks/plan`);
    } else {
      await api.post(`/workspaces/${workspaceId}/tasks/plan`);
      // Apply Method决定是否自动创建Apply任务
    }
    
    // 3. 跳转回workspace
    navigate(`/workspaces/${workspaceId}?tab=runs`);
  };
};
```

## 🔌 API设计

### 现有API（无需修改）

```
# 创建Plan任务
POST /api/v1/workspaces/:id/tasks/plan

# 创建Apply任务
POST /api/v1/workspaces/:id/tasks/apply

# 添加资源
POST /api/v1/workspaces/:id/resources
{
  "resource_type": "aws_s3_bucket",
  "resource_name": "my_bucket",
  "tf_code": {...},
  "variables": {...}
}
```

### 可选的批量API

```
# 批量添加资源
POST /api/v1/workspaces/:id/resources/batch
{
  "resources": [
    {
      "resource_type": "aws_s3_bucket",
      "resource_name": "my_bucket",
      "tf_code": {...}
    },
    {
      "resource_type": "aws_vpc",
      "resource_name": "main",
      "tf_code": {...}
    }
  ],
  "run_type": "plan" | "plan_and_apply"
}
```

## 📋 实施计划

### Phase 1: New Run对话框（1天）
- [ ] 创建NewRunDialog组件
- [ ] 实现3个选项的UI
- [ ] 实现Plan和Plan+Apply的直接执行
- [ ] 实现跳转到添加资源页面

### Phase 2: 添加资源页面（2-3天）
- [ ] 创建AddResourcesPage组件
- [ ] 实现Module选择界面
- [ ] 集成DynamicForm组件
- [ ] 实现预览界面
- [ ] 实现提交逻辑

### Phase 3: 版本管理集成（1天）
- [ ] 确保资源添加时创建版本
- [ ] 确保任务执行时使用最新版本
- [ ] 测试版本管理流程

### Phase 4: 测试和优化（1天）
- [ ] 端到端测试
- [ ] 用户体验优化
- [ ] 错误处理完善

## 🎓 关键点

### 1. 版本管理

**创建新版本的时机**：
-  添加新资源
-  修改现有资源
-  回滚资源
- ❌ 直接执行Plan
- ❌ 直接执行Plan+Apply

### 2. Apply Method

**Auto apply = On**：
- Plan成功后自动创建Apply任务

**Auto apply = Off**：
- Plan成功后等待用户手动创建Apply任务

### 3. 资源来源

**只能从Module库添加**：
- 不支持手动编写Terraform代码
- 必须选择已有的Module
- 通过动态表单填写配置

### 4. 表单复用

**复用现有组件**：
- DynamicForm组件
- Schema渲染逻辑
- 表单验证逻辑

## 🔗 相关文档

- [17-resource-level-version-control.md](./17-resource-level-version-control.md) - 资源版本管理设计
- [15-terraform-execution-detail.md](./15-terraform-execution-detail.md) - Terraform执行流程

---

**下一步**: 开始实现New Run对话框组件
